---
phase: 01-aws-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [infrastructure/terraform/cloudfront.tf]
autonomous: true
requirements: [INFRA-04, INFRA-05]

must_haves:
  truths:
    - "CloudFront distribution defined with learn.chronopoli.io alias"
    - "ACM certificate defined with wildcard *.chronopoli.io"
    - "Static assets have separate cache behavior with long TTL"
    - "Viewer protocol set to redirect-to-https"
  artifacts:
    - path: "infrastructure/terraform/cloudfront.tf"
      provides: "CDN distribution with SSL and cache behaviors"
      min_lines: 50
      contains: "aws_cloudfront_distribution"
  key_links:
    - from: "infrastructure/terraform/cloudfront.tf"
      to: "infrastructure/terraform/main.tf"
      via: "Origin uses EC2 Elastic IP"
      pattern: "aws_eip\\.chronopoli\\.public_ip"
---

<objective>
Validate CloudFront CDN and SSL certificate Terraform configuration.

Purpose: Ensure CDN serves learn.chronopoli.io with proper SSL, caching, and HTTPS redirect.
Output: Validated cloudfront.tf ready for provisioning.
</objective>

<execution_context>
@/Users/christianschmitt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christianschmitt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@infrastructure/terraform/cloudfront.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate CloudFront and ACM configuration</name>
  <files>infrastructure/terraform/cloudfront.tf</files>
  <read_first>infrastructure/terraform/cloudfront.tf, docs/01-aws-infrastructure.md</read_first>
  <action>Verify CloudFront distribution: (1) Origin points to EC2 Elastic IP with https-only, (2) Aliases include learn.chronopoli.io, (3) Viewer protocol redirects to HTTPS, (4) Default TTL is 86400, (5) Static assets path /static/* has separate cache behavior with 604800 TTL, (6) All methods forwarded (GET/POST/PUT/DELETE etc.), (7) Cookies forwarded for LMS sessions. Verify ACM cert: wildcard *.chronopoli.io, DNS validation, in us-east-1 provider. Fix the us_east_1 provider alias — it must be defined before use.</action>
  <verify>grep -c "aws_cloudfront_distribution\|aws_acm_certificate\|redirect-to-https\|/static/" infrastructure/terraform/cloudfront.tf shows all 4 patterns</verify>
  <acceptance_criteria>
    - cloudfront.tf contains aws_cloudfront_distribution resource
    - cloudfront.tf contains aws_acm_certificate with "*.chronopoli.io"
    - cloudfront.tf contains viewer_protocol_policy = "redirect-to-https"
    - cloudfront.tf contains ordered_cache_behavior for "/static/*"
    - cloudfront.tf uses provider = aws.us_east_1 for ACM certificate
  </acceptance_criteria>
  <done>CloudFront and SSL configuration correct and complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CloudFront distribution has correct aliases and origin
- [ ] ACM certificate covers wildcard domain
- [ ] HTTPS redirect enabled
- [ ] Static assets cached separately
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- CDN layer ready for provisioning
</success_criteria>

<output>
After completion, create `.planning/phases/01-aws-infrastructure/01-03-SUMMARY.md`
</output>
