---
phase: 01-aws-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [infrastructure/terraform/main.tf, infrastructure/terraform/variables.tf, infrastructure/terraform/outputs.tf]
autonomous: true
requirements: [INFRA-01, INFRA-06]
user_setup:
  - service: aws
    why: "Terraform needs AWS credentials to provision resources"
    env_vars:
      - name: AWS_ACCESS_KEY_ID
        source: "AWS Console → IAM → Users → chronopoli-deployer → Security credentials"
      - name: AWS_SECRET_ACCESS_KEY
        source: "AWS Console → IAM → Users → chronopoli-deployer → Security credentials"
    dashboard_config:
      - task: "Create SSH key pair"
        location: "AWS Console → EC2 → Key Pairs → Create key pair"
        details: "Name: chronopoli-production-key, Type: RSA, Format: .pem"

must_haves:
  truths:
    - "terraform validate passes with no errors"
    - "terraform plan completes without syntax errors"
    - "EC2 instance defined with t3.xlarge and Ubuntu 22.04 AMI"
    - "Security group allows ports 22, 80, 443 inbound"
  artifacts:
    - path: "infrastructure/terraform/main.tf"
      provides: "EC2, security groups, IAM, EBS volumes, Elastic IP"
      min_lines: 100
      contains: "aws_instance"
    - path: "infrastructure/terraform/variables.tf"
      provides: "All configurable parameters"
      contains: "ec2_instance_type"
    - path: "infrastructure/terraform/outputs.tf"
      provides: "Resource identifiers for downstream use"
      contains: "ec2_public_ip"
  key_links:
    - from: "infrastructure/terraform/main.tf"
      to: "infrastructure/terraform/variables.tf"
      via: "var references"
      pattern: "var\\.ec2_instance_type"
    - from: "infrastructure/terraform/main.tf"
      to: "infrastructure/terraform/s3.tf"
      via: "IAM policy referencing S3 bucket ARNs"
      pattern: "aws_s3_bucket\\.media\\.arn"
---

<objective>
Validate and finalize EC2 + Security Groups + IAM Terraform configuration.

Purpose: Ensure Terraform code for compute, networking, and IAM is correct and ready for `terraform apply`.
Output: Validated main.tf, variables.tf, outputs.tf passing `terraform validate`.
</objective>

<execution_context>
@/Users/christianschmitt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christianschmitt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@infrastructure/terraform/main.tf
@infrastructure/terraform/variables.tf
@infrastructure/terraform/outputs.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Terraform syntax and fix any issues</name>
  <files>infrastructure/terraform/main.tf, infrastructure/terraform/variables.tf</files>
  <read_first>infrastructure/terraform/main.tf, infrastructure/terraform/variables.tf, infrastructure/terraform/outputs.tf</read_first>
  <action>Run `terraform -chdir=infrastructure/terraform init -backend=false` then `terraform -chdir=infrastructure/terraform validate`. Fix any syntax errors, missing variable references, or provider issues. Ensure the `aws.us_east_1` provider alias is defined in cloudfront.tf (not duplicated in main.tf). Verify all variable references in main.tf match declarations in variables.tf.</action>
  <verify>terraform -chdir=infrastructure/terraform validate returns "Success! The configuration is valid."</verify>
  <acceptance_criteria>
    - terraform validate exits with code 0
    - No "undeclared variable" errors
    - No "duplicate provider" errors
  </acceptance_criteria>
  <done>Terraform configuration validates without errors</done>
</task>

<task type="auto">
  <name>Task 2: Verify resource completeness against docs/01-aws-infrastructure.md</name>
  <files>infrastructure/terraform/main.tf</files>
  <read_first>docs/01-aws-infrastructure.md, infrastructure/terraform/main.tf, infrastructure/terraform/rds.tf, infrastructure/terraform/s3.tf, infrastructure/terraform/cloudfront.tf</read_first>
  <action>Cross-reference every AWS resource documented in docs/01-aws-infrastructure.md against the Terraform files. Verify: (1) EC2 t3.xlarge with Ubuntu 22.04, (2) Security group with ports 22/80/443, (3) EBS data volume 100GB gp3, (4) Elastic IP, (5) IAM role with S3 access policy. Add any missing resources or fix mismatches. Ensure user_data script installs Docker and Tutor.</action>
  <verify>grep -c "aws_instance\|aws_security_group\|aws_eip\|aws_ebs_volume\|aws_iam_role" infrastructure/terraform/main.tf shows all 5 resource types present</verify>
  <acceptance_criteria>
    - main.tf contains "aws_instance" resource with instance_type referencing var.ec2_instance_type
    - main.tf contains security group with ingress rules for ports 22, 80, 443
    - main.tf contains aws_ebs_volume with var.ec2_data_volume_size
    - main.tf contains aws_eip resource
    - main.tf contains aws_iam_role with S3 access policy
  </acceptance_criteria>
  <done>All documented AWS resources exist in Terraform with correct configuration</done>
</task>

<task type="auto">
  <name>Task 3: Generate terraform plan output for review</name>
  <files>infrastructure/terraform/production.tfvars.example</files>
  <read_first>infrastructure/terraform/production.tfvars.example, infrastructure/terraform/variables.tf</read_first>
  <action>Verify production.tfvars.example contains all required variables from variables.tf. Ensure sensitive variables (rds_password) have placeholder values. Add comments explaining each section. Run `terraform -chdir=infrastructure/terraform plan -var-file=production.tfvars.example` to verify plan generation (this will fail without AWS credentials but validates variable completeness).</action>
  <verify>All variables in variables.tf that lack defaults are present in production.tfvars.example</verify>
  <acceptance_criteria>
    - production.tfvars.example contains ec2_key_name value
    - production.tfvars.example contains rds_password with "CHANGE_THIS" placeholder
    - Every required variable (no default) in variables.tf has a corresponding entry in production.tfvars.example
  </acceptance_criteria>
  <done>tfvars example is complete with all required variables documented</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] terraform validate passes
- [ ] All 5 core resource types present in main.tf
- [ ] production.tfvars.example covers all required variables
- [ ] No hardcoded secrets in any .tf file
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Terraform configuration is ready for `terraform apply` once AWS credentials are provided
</success_criteria>

<output>
After completion, create `.planning/phases/01-aws-infrastructure/01-01-SUMMARY.md`
</output>
