---
phase: 01-aws-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [infrastructure/terraform/rds.tf, infrastructure/terraform/s3.tf]
autonomous: true
requirements: [INFRA-02, INFRA-03]

must_haves:
  truths:
    - "RDS MySQL 8.0 defined with Multi-AZ and encrypted storage"
    - "3 S3 buckets defined: media, static, backups"
    - "Backup bucket has lifecycle policy with Glacier transition"
    - "Media bucket has public read policy"
  artifacts:
    - path: "infrastructure/terraform/rds.tf"
      provides: "RDS MySQL with subnet group and security group"
      min_lines: 40
      contains: "aws_db_instance"
    - path: "infrastructure/terraform/s3.tf"
      provides: "S3 buckets with policies, versioning, lifecycle"
      min_lines: 80
      contains: "aws_s3_bucket"
  key_links:
    - from: "infrastructure/terraform/rds.tf"
      to: "infrastructure/terraform/main.tf"
      via: "Security group reference for EC2 access"
      pattern: "aws_security_group\\.chronopoli\\.id"
---

<objective>
Validate RDS and S3 Terraform configuration for data layer.

Purpose: Ensure database and storage infrastructure is correctly defined for OpenEdX data persistence.
Output: Validated rds.tf and s3.tf ready for provisioning.
</objective>

<execution_context>
@/Users/christianschmitt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/christianschmitt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@infrastructure/terraform/rds.tf
@infrastructure/terraform/s3.tf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate RDS configuration against docs spec</name>
  <files>infrastructure/terraform/rds.tf</files>
  <read_first>infrastructure/terraform/rds.tf, docs/01-aws-infrastructure.md, infrastructure/terraform/main.tf</read_first>
  <action>Verify RDS resource matches docs/01-aws-infrastructure.md spec: MySQL 8.0, db.t3.medium, Multi-AZ, 50GB gp3 auto-scaling to 200GB, encrypted, 7-day backup retention. Verify security group only allows MySQL port 3306 from EC2 security group (no public access). Verify subnet group uses default VPC subnets.</action>
  <verify>grep -c "engine.*mysql\|multi_az\|storage_encrypted\|backup_retention" infrastructure/terraform/rds.tf shows all 4 settings present</verify>
  <acceptance_criteria>
    - rds.tf contains engine = "mysql" with engine_version = "8.0"
    - rds.tf contains multi_az = var.rds_multi_az
    - rds.tf contains storage_encrypted = true
    - rds.tf contains backup_retention_period = 7
    - RDS security group ingress limited to aws_security_group.chronopoli.id
  </acceptance_criteria>
  <done>RDS configuration matches documented architecture</done>
</task>

<task type="auto">
  <name>Task 2: Validate S3 buckets with policies and lifecycle</name>
  <files>infrastructure/terraform/s3.tf</files>
  <read_first>infrastructure/terraform/s3.tf, docs/01-aws-infrastructure.md</read_first>
  <action>Verify 3 S3 buckets exist: media (public read + versioning), static (public read), backups (private + versioning + lifecycle + KMS encryption). Verify media bucket policy allows s3:GetObject for Principal "*". Verify backup lifecycle transitions to Glacier after 30 days and expires after 365 days. Verify bucket names follow pattern: chronopoli-{type}-{environment}.</action>
  <verify>grep -c "aws_s3_bucket\"" infrastructure/terraform/s3.tf shows 3 buckets defined</verify>
  <acceptance_criteria>
    - s3.tf defines exactly 3 aws_s3_bucket resources (media, static, backups)
    - Media bucket has public access block set to allow public policy
    - Media bucket has s3:GetObject policy for Principal "*"
    - Backups bucket has lifecycle rule with GLACIER transition at 30 days
    - Backups bucket has server-side encryption with aws:kms
  </acceptance_criteria>
  <done>All S3 buckets correctly configured with security and lifecycle policies</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] RDS has all 4 key settings (MySQL, Multi-AZ, encrypted, 7-day backup)
- [ ] 3 S3 buckets defined with correct policies
- [ ] No public access to RDS
- [ ] Backup bucket encrypted with KMS
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Data layer infrastructure ready for provisioning
</success_criteria>

<output>
After completion, create `.planning/phases/01-aws-infrastructure/01-02-SUMMARY.md`
</output>
